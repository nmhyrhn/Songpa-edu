<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper"> <!--이 파일의 고유 이름표 -->
    <select id="selectSysdate" resultType="java.util.Date">
        SELECT CURDATE()
    </select>
</mapper>